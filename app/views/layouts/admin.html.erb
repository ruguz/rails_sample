<!DOCTYPE html>
<html>
<head>
  <title>【<%= Rails.env %>】Admin</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="#">【<%= Rails.env %>】 Admin</a>
      </div>
      <p class="navbar-text navbar-right">ログイン中： <%= current_admin_user.name %></p>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-3 col-md-2">
        <%= render 'admin/common/sidemenu' %>
      </div>
      <div class="col-sm-9 col-md-10">
        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>
